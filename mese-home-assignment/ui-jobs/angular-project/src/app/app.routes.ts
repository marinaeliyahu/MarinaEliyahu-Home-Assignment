import { Routes } from '@angular/router';
import { JobsTableComponent } from './jobs-table/jobs-table.component';

//export const routes: Routes = [];

export const routes: Routes = [
    { path: 'app-jobs-table', component: JobsTableComponent }
     
  ];